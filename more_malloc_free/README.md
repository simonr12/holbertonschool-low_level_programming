0-malloc_checked.c Allocates memory using `malloc`, exits with status `98` on failure
1-string_nconcat.c Concatenates two strings, using at most `n` bytes of `s2`
2-calloc.c  Allocates memory for an array, initializes with zero (like `calloc`)
3-array_range.c Creates an array of integers from `min` to `max`