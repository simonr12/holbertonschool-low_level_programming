0-create_array.c` Creates an array of chars, initialized with a specific char
1-strdup.c` Returns a pointer to a newly allocated space in memory containing a copy of a string
2-str_concat.c` Concatenates two strings into newly allocated memory
3-alloc_grid.c` Returns a pointer to a 2D array of integers
4-free_grid.c` Frees a 2D grid previously allocated